懒鬼使用手册

本手册讲如何最省力地使用和维护「我的知识花园」这个网站。
网站地址：https://my-knowledge.rafguy329.workers.dev

一、打开网站

(一) 电脑
1. 先打开代理软件。网站放在 workers.dev 上，国内直连打不开，这是网络问题，不是网站坏了。
2. 浏览器打开 https://my-knowledge.rafguy329.workers.dev

(二) 手机
1. 开梯子后，浏览器打开同一个网址即可。
2. 以后绑定自己的域名就不需要代理了（见六、(四)）。

(三) 顺手会用的小功能
1. 站内搜索：键盘按 Ctrl+K，输入关键词。
2. 暗色模式：页头有切换按钮。
3. 订阅更新：把 https://my-knowledge.rafguy329.workers.dev/rss.xml 加进任意 RSS 阅读器，有新笔记会收到提醒。

二、写一篇新笔记

(一) 文件放哪
1. 所有笔记都在 D:\my-knowledge\src\content\notes\ 里，一篇笔记一个 .md 文件。
2. 文件名用英文小写加短横线，例如 reading-notes.md。中文标题写在文件内部（见下面 title 一行）。

(二) 笔记格式
新建文件后，把下面这段复制进去，再改内容：

title: 这里写中文标题
description: 一句话介绍，列表页会显示
pubDate: 2026-08-25
updatedDate: 2026-08-25
category: 方法论
tags: [笔记, 学习]
status: learning

这里开始写正文。

(三) 每个字段的意思
1. title：必填。页面上显示的标题，写中文。
2. pubDate：必填。发布日期，格式 年-月-日。
3. description：一句话介绍，显示在列表和订阅里。
4. updatedDate：最近修改日期，列表按它排序。不填就用 pubDate。
5. category：分类，写一个词，中文英文都行。
6. tags：标签，一列词，用方括号加逗号，如 [A, B]。可以写多个。
7. status：状态，只能写下面四个英文词之一：
(1) learning —— 学习中
(2) organizing —— 整理中（不写这一行时默认是它）
(3) mastered —— 已掌握
(4) archived —— 归档（归档后从首页和列表隐藏）

(四) 正文里能用什么
1. 普通 Markdown 都能用：井号表示标题、短横线表示列表、代码块、表格等。
2. 双链：写 [[另一篇笔记的文件名]]，就能连到那篇笔记；想换显示文字就写 [[文件名|显示文字]]。
3. 图片：把图片放进 public\images\ 文件夹，正文写 ![图片说明](/images/图片文件名.png)。
4. 完整语法参考站内笔记《Markdown 常用语法速查》。

三、分类和标签

(一) 加新分类
1. 在某篇笔记的 category: 后面写一个新词，发布后自动出现新的分类页。
2. 没写 category 的笔记自动归入「未分类」。

(二) 加新标签
1. 在某篇笔记的 tags: 里写新词，发布后自动出现新的标签页。

(三) 一个提醒
1. 修改已发布笔记的分类或标签，会改变它的网址，别人存的旧链接会失效。定名字时想好再发布。

四、发布（每次写完必做，固定三条命令）

(一) 在 D:\my-knowledge 文件夹里打开终端（Git Bash 或 VS Code 里的终端），依次执行：
1. git add .
2. git commit -m "新笔记：xxx"
3. git push

(二) 翻译成人话
1. git add . —— 把所有改动打包。
2. git commit —— 给这次改动记一笔说明（引号里随便写）。
3. git push —— 上传到 GitHub，网站约一分钟后自动更新。

(三) 常见情况
1. push 报连不上网：代理没开，开了再试一次。
2. 弹登录窗口：用 GitHub 账号登录即可。
3. 想确认发布成功：等一分钟后刷新网站；或者到 Cloudflare 后台 Workers & Pages 里 my-knowledge 的部署页，看最新一次构建是否成功。

五、换电脑或重装系统

(一) 第一次准备
1. 装 Node.js（官网下载 LTS 版）和 git。
2. 打开终端执行 git clone https://github.com/Ra2fSt/my-knowledge.git。clone 失败就先开代理再试。
3. 执行 cd my-knowledge，然后 npm install。
4. 完成后照第四节正常写、正常发布。

(二) 想先在本地看看效果
1. 执行 npm run dev，浏览器打开 http://localhost:4321。
2. 本地预览时中文分类页可能打不开，这是已知小 bug，不影响线上网站，详见 docs\pitfalls.md。

六、进阶（可以以后再看）

(一) 改站名和介绍：编辑 src\consts.ts。
(二) 改配色：编辑 src\styles\tokens.css。注意：文件里有两个暗色块，必须一起改，改完对照检查。
(三) 改每页篇数、图谱节点上限等：先看 PROJECT_STATUS.md 第 8 节的约定，再动手。
(四) 绑定自己的域名：解决国内直连问题。准备好域名后随时找我。

七、出问题怎么办

1. 网站没更新：去 Cloudflare 后台看构建日志（Workers & Pages → my-knowledge → 部署记录），八成是构建报错了。
2. 构建报错：把报错日志发给我。
3. 遇到诡异问题：先翻 docs\pitfalls.md，本项目踩过的坑都记在里面。
4. 实在不行：把报错贴给 Claude Code。
